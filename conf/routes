# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /mlib                       controllers.Application.mlib
GET     /simple                     controllers.Application.simple
GET     /twitter                    controllers.Application.twitter
GET     /sql                        controllers.Application.sql
GET     /donations                  controllers.Application.donations

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
